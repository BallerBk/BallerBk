pandas>=2.0
numpy>=1.24
scipy>=1.10
PyYAML>=6.0

# Optional, only needed if you set data_provider: yfinance in config.yaml
# (requires normal internet access -- this sandbox blocks market-data domains)
yfinance>=0.2.40

# Optional, only needed to run the test suite
pytest>=7.4
